#include<iostream>
using namespace std;
#include "string.h"


char* getstr()(char&,int)
int main()
{
    int n1,n2;
    scanf("%d%d",&n1,&n2);
    char *s1,*s2,*s3;
    s1=(char *)calloc(n1,Sizeof(char));
    s2=(char *)calloc(n2,Sizeof(char));
    s3=(char *)calloc(')
}
